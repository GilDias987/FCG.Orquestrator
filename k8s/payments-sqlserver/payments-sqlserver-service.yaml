apiVersion: v1
kind: Service
metadata:
  name: payments-sqlserver
  labels:
    app: payments-sqlserver
spec:
  type: ClusterIP
  selector:
    app: payments-sqlserver
  ports:
    - protocol: TCP
      port: 1433
      targetPort: 1433
