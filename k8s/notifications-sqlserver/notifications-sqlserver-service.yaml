apiVersion: v1
kind: Service
metadata:
  name: notifications-sqlserver
  labels:
    app: notifications-sqlserver
spec:
  type: ClusterIP
  selector:
    app: notifications-sqlserver
  ports:
    - protocol: TCP
      port: 1433
      targetPort: 1433
