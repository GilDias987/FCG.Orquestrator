apiVersion: apps/v1
kind: Deployment
metadata:
  name: notifications-sqlserver
  labels:
    app: notifications-sqlserver
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notifications-sqlserver
  template:
    metadata:
      labels:
        app: notifications-sqlserver
    spec:
      containers:
        - name: sqlserver
          image: mcr.microsoft.com/mssql/server:2022-latest
          ports:
            - containerPort: 1433
              protocol: TCP
          env:
            - name: ACCEPT_EULA
              value: "Y"
            - name: MSSQL_PID
              value: "Developer"
            - name: SA_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: notifications-sqlserver-secret
                  key: SA_PASSWORD
          volumeMounts:
            - name: sqlserver-data
              mountPath: /var/opt/mssql
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "2Gi"
              cpu: "1000m"
      volumes:
        - name: sqlserver-data
          persistentVolumeClaim:
            claimName: notifications-sqlserver-pvc
