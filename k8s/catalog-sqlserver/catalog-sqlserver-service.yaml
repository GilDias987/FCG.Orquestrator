apiVersion: v1
kind: Service
metadata:
  name: catalog-sqlserver
  labels:
    app: catalog-sqlserver
spec:
  type: ClusterIP
  selector:
    app: catalog-sqlserver
  ports:
    - protocol: TCP
      port: 1433
      targetPort: 1433
