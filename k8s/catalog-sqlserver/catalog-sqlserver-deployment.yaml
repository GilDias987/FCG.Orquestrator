apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-sqlserver
  labels:
    app: catalog-sqlserver
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catalog-sqlserver
  template:
    metadata:
      labels:
        app: catalog-sqlserver
    spec:
      containers:
        - name: sqlserver
          image: mcr.microsoft.com/mssql/server:2022-latest
          ports:
            - containerPort: 1433
              protocol: TCP
          env:
            - name: ACCEPT_EULA
              value: "Y"
            - name: MSSQL_PID
              value: "Developer"
            - name: SA_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: catalog-sqlserver-secret
                  key: SA_PASSWORD
          volumeMounts:
            - name: sqlserver-data
              mountPath: /var/opt/mssql
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "2Gi"
              cpu: "1000m"
      volumes:
        - name: sqlserver-data
          persistentVolumeClaim:
            claimName: catalog-sqlserver-pvc
